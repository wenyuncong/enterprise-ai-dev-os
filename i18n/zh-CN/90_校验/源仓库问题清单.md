# 源仓库问题清单 | Source Repository Findings

> **⚠️ 先读这条定性**：本文件是**通读记录**，**不是缺陷判决书**。逐条裁定（哪些真实、哪些降级为文档级、**哪 3 条是我自己的误报已撤回**）见同目录 [`源问题复核结论.md`](源问题复核结论.md)。
> **客观基线**：英文源不做任何改动时，仓库自己的 **10 道门禁全部 exit 0**（方法论审计 0 失败 0 警告、49 个 Skill 平均 100.0/100、就绪度 100/100）。因此本清单描述的是**文档一致性与呈现质量**，不是“跑不起来”的功能缺陷。
>
> **文档类型**：翻译过程中发现的源仓库自身问题（不是翻译问题）
> **产生方式**：本轮中文版翻译由多个译者并行通读英文源，逐条记录异常；本文件收录**经本轮二次核实（Test-Path / 正则 / 注册表读取 / 代码逻辑阅读）确认存在**的条目
> **核实日期**：2026-09-14
> **重要**：本轮**未修改任何英文源文件**（142 个受保护源文件哈希与基线一致，见 `校验报告.md`）。本清单仅供源仓库维护者决策。

---

## A. 结论类问题（会影响读者判断，建议优先修）

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| A1 | **CodeBuddy 的 Skill 支持级别自相矛盾** | `docs/COMPATIBILITY.md:25` 把 CodeBuddy 的 Skills 标为 `Experimental`；`tools/adapters.json` 中 `tools.codebuddy.skills.status = "verified"`；`docs/TOOL_ADAPTERS.md:27` 也标 `Verified`、层级 P1 | 以注册表（adapters.json）为准还是以兼容矩阵为准，需二选一并统一 |
| A2 | **CodeBuddy 漏出推广优先级表** | `docs/TOOL_ADAPTERS.md:27` 定级 P1；`docs/COMPATIBILITY.md:54` 的 P1 行只有 Cursor、GitHub Copilot、VS Code | 把 CodeBuddy 补进 `COMPATIBILITY.md` 的 P1 行，或把 TOOL_ADAPTERS 降级 |
| A3 | ~~`AGENTS.md` 步骤 10 的部署命令与实际门禁不符~~ **【已撤回，见复核结论第 2 节】** | `tools/deploy.ps1:115-117` 的 `all` 分支直接返回全部适配器，状态过滤只作用于 `verified`/`verified-only`，故 `AGENTS.md:559` 的描述准确；仅 `-IncludePending` 的帮助文字略不精确 | 无需修改 |
| A4 | **`AGENTS.md` 0.0 的适配器目录清单不全** | `AGENTS.md:22` 只列 `.agents/`、`.trae/`、`.qoder/`、`.claude/`、`.codebuddy/`；`tools/adapters.json` 实际注册 **16** 个适配器（另有 `.cursor/`、`.github/`、`.windsurf/`、`.cline/`、`.roo/`、`.continue/`、`.lingma/` 等） | 改为「以 `tools/adapters.json` 为准」，避免清单漂移 |
| A5 | ~~交付职责矩阵只覆盖 25/49 个 Skill~~ **【已撤回：设计如此，见复核结论第 2 节】** | 矩阵首部自述「每个交付阶段一个主责 Skill」，是阶段视图而非 Skill 目录，并明确不替代技术栈 Skill 与领域验收 | 无需修改 |
| A6 | **`task_contract.json` 模板本身跑不通 L2/L3 闭环** | 模板字段只有 `contract_id/schema_version/status/project_id/delivery/product_contract/truth_owner/scope/test_strategy/evidence_plan/reviews/implementer/owner_confirmation/created_at/updated_at`，**无 `verification_owner`**；而 `scripts/py/validate_delivery_contract.py:127` 要求 L2/L3 的 `safeguarded`/`completed` 必须有与 `implementer` 不同的 `verification_owner`，否则报 `CONTRACT_INDEPENDENT_VERIFIER` | 在模板中补齐 `verification_owner` 字段（或注明必须手工添加） |
| A7 | **契约字段命名三处不一致** | `AGENTS.md:129` 用 `public test seam`、`proof plan`；`docs/全项目总控/schemas/governance/delivery-contract.schema.json` 实际键为 `evidence_plan`、`test_strategy.public_seam`；`ai-delivery-contract-governor` 用 `public_test_seam` | 在源侧统一，或在 AGENTS.md 给出「口径词 → 实际键名」映射表 |

> 备注：矩阵中**没有出现任何不属于 49 个正式 Skill 的名字**，即不存在“矩阵提到但不在正式清单内”的问题。

## B. 文档一致性与时效性

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| B1 | **Roadmap 状态滞后** | `docs/公开材料/ROADMAP.md` 把 `Value evidence template`、`Rule Runtime Lite design` 标为 Planned，但 `docs/公开材料/VALUE_EVIDENCE.md` 与 `RULE_RUNTIME_LITE.md` 均已交付（后者自述“设计完成、未实现”） | 状态改为「设计完成 / 实现中」 |
| B2 | **历史快照的技能数与现状冲突** | `docs/公开材料/VALUE_EVIDENCE.md:76` 写 `42 skills`；当前 `skills/SKILL_MANIFEST.json` 的 `counts.official = 49`（core 16 / governance 13 / tech 20），`AGENTS.md` 亦写 49 | 加注「截至该合作期」，或更新为该期真实数字 |
| B3 | **Trae 与 Trae Solo 共用目标目录有覆盖风险** | 两者 rules/skills 目标同为 `.trae/rules/project_rules.md` 与 `.trae/skills`；`tools/adapters.json` 备注要求二者隔离，但 `docs/TOOL_ADAPTERS.md` 未提示 | 在 TOOL_ADAPTERS 增加风险提示 |
| B4 | **适配器条目口径漂移** | `docs/TOOL_ADAPTERS.md` 15 行（Copilot/VS Code 合并、Qoder/Qoder CN 合并），`tools/adapters.json` 为 16 个 key（`copilot`、`vscode` 分列）；文档写 `.github/instructions/*.instructions.md`（通配），注册表固定为 `enterprise-ai-dev-os.instructions.md` | 文档与注册表对齐，或注明合并规则 |
| B5 | **`05` 号方法论文档前置信息笔误** | `methodology/06_企业级部署与验收标准.md` 的前置信息写 `03_13步开发执行引擎`，实际文件名为 `methodology/03_12步开发执行引擎.md` | 修正笔误（或按 C1 统一改名） |

## C. 源文件结构性缺陷（影响渲染与阅读）

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| C1 | **文件名与内容不一致：03 号文档写「12步」，内容实为 13 步** | 源文件 `methodology/03_12步开发执行引擎.md` 的正文标题为 `## 2. The 13 Steps`，含 Step 1–13，验证清单也是 13 条；**`docs/全项目总控/MASTER_INDEX.md` 第 34 行也写作「12 步开发执行引擎」**，`methodology/06` 的前置信息则写 `03_13步开发执行引擎` —— 三处口径不一致 | 改名或改标题，二者取一，并同步 MASTER_INDEX |
| C2 | **03 号文档 Step 13 位置异常** | Step 13（浏览器运行时验证）排在 `## 3. Verification Checklist` **之后**，不在 `## 2. The 13 Steps` 内；且 Step 1–12 有「后端/前端/联调」阶段小标题，Step 13 没有「验证阶段」小标题 | 把 Step 13 移入第 2 节并补阶段小标题 |
| C3 | **01 号文档章节编号重复与跳号** | 同时存在 `## 6. Layer 4: Evolution` 与 `## 6. Governance Layer (Horizontal)`；`## 7.` 直接跳到 `## 9.`（缺 8） | 重排编号 |
| C4 | **01 号文档五层架构图缺 Layer 0** | ASCII 图只有 LAYER 1–4，与标题「五层架构」不符；图位置在 §2 之后而非 §1 | 补 Layer 0 画框并移到 §1 |
| C5 | **01 号文档内容自相矛盾** | §5 把 `ai-task-decomposer` 列为执行层 Skill，而 §4 明确它是第 2 层（分解层）所有者；§6 称「四个纵向层」与五层表述不一 | 统一层级归属 |
| C6 | **多篇文档用单反引号当代码围栏** | `methodology/00`、`01`、`02`、`03`、`04`、`05`、`06`，以及 `AGENTS.md` 第 4/9/11 节，均存在单反引号（或 `` `bash ``）起始的伪围栏，在标准 Markdown 下渲染异常 | 统一改为三反引号 |
| C7 | **`AGENTS.md` 第 3 节有两条近乎重复的 `⚠️ CRITICAL`** | 两条都要求 Step 13 必须通过（第一条额外提到 Step 13a） | 合并去重 |
| C8 | **`ai-atomic-governance` 结构最薄** | 无 `## Trigger`、无 `## Guardrails`、无 Maturity/Evolution History，且代码围栏用单反引号不闭合；`skills/SKILL_MANIFEST.json` 中其成熟度为 `callable`（非 verified） | 补齐结构或调整成熟度口径 |
| C9 | **`ai-cross-project-audit` 同样缺节** | 无 `## Trigger`、无 `## Guardrails`、无 Maturity/Evolution History | 同上 |
| C10 | **5 个 governance Skill 的 `## Rule` 位于 H1 之前** | `ai-component-standardizer`、`ai-flow-closure-audit`、`ai-frontend-audit`、`ai-reference-researcher`、`ai-runtime-verify` | 结构倒置，建议统一为 H1 → Rule |
| C11 | **`ai-5s-delivery-governor` 标题语言与符号不统一** | 唯一双语标题是 `## Delivery Qualification \| 交付资格`，其余为纯英文；H1 用连字符 `-` 而非破折号 | 统一标题风格 |
| C12 | **5S Skill 提到 `DeliveryContract` 但未给模板路径** | `ai-5s-delivery-governor` 的 Standard Workflow 要求创建并校验 `DeliveryContract`，未指向 `docs/_templates/全项目总控/task_contract.json` | 补链接 |
| C13 | **`BUSINESS_PROCESS_TEMPLATE.md` 的 Mermaid 图用了单反引号围栏** | `docs/_templates/业务流程全案/BUSINESS_PROCESS_TEMPLATE.md:17` 是 `` `mermaid ``、`:26` 是 `` ` ``，标准 Markdown 不会渲染为代码块，Mermaid 图直接失效 | 改为三反引号 |

## D. 与审计脚本口径相关的观察

| # | 观察 | 证据 |
|---|---|---|
| D1 | ~~`audit_methodology.py` 对 AGENTS.md 要求的部分英文断言词在 AGENTS.md 中并不存在~~ **【已撤回，见复核结论第 2 节】** | 实测 `saleability`、`atomic orchestration`、`safe change`、`exact task-owned` 在 `AGENTS.md` 中出现 0 次，但这四个词本就要求在**两个 Skill** 内出现（`saleability` 在 5S Skill 中 1 次），审计因此 PASS；校验脚本已改为「以英文源为准」判定 | 无需修改 |
| D2 | `audit_skill_health.py` 的中文反模式正则会误伤写得很规范的中文禁令 | 实测「前端不得计算金额」「前端禁止做业务校验」「禁止临时绕过检查」均命中 `ANTI_PATTERNS`，各扣 10 分。因此若把中文版正文放进 `skills/` 被扫描，结构健康分会下降 |
| D3 | `rule_lint.py` 会扫描盘符路径与根目录散文件 | 中文版因此把「本机盘符路径」列为绝对禁止项；本轮校验脚本对中文版全量扫描，残留 0 |
| D4 | 该中文反模式误报**真实发生过**：本轮译文 `10_规则/AGENTS_中文版.md` 命中 2 处 | 规范写法的中文禁令（「前端……计算/校验」否定句、「临时绕过」字面量）被判为反模式。中文版之所以不受影响，是因为它放在 `skills/` 之外、不被 `audit_skill_health.py` 扫描；若把中文正文复制进 `skills/`，结构健康分会下降 |

---

## E. 自研 Skill 正文层面的缺陷（逐条已核实）

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| F1 | **4 处脏字符/拼接残留** | `skills/core/ai-command-executor/SKILL.md:142` → `orchestrationr`（应为 `orchestration`，且其后有一行孤立的 `r`）；`skills/core/ai-skill-evolver/SKILL.md:148` → `validatedr`；`skills/core/ai-rule-dispatcher/SKILL.md:151` → `dispatchesr`；`skills/core/ai-library-first/SKILL.md:175` → `project''s`（双单引号） | 批量清理 |
| F2 | **同一文件内部数值自相矛盾** | `skills/core/ai-skill-governor/SKILL.md:16` 写 "49 official skills"，同文件 `:136` 的审计输出模板写 `\| Total official skills \| 42 \|` | 模板示例值改为 49 或写成占位符 |
| F3 | **成熟度两套量表并存** | `skills/SKILL_MANIFEST.json` 的 `maturityLevels` 为 `raw/generalized/callable/verified/deprecated`，5 个 core Skill 的 `maturity=verified`；但对应 SKILL.md 的 `## Maturity` 自述为 `New`/`Effective`，而 `ai-skill-evolver` 又用第三套五级 `declared/callable/effective/in-closure/evolving` | 统一为一套量表，或明确两套的分工 |
| F4 | **引用不存在的历史 Skill 名与小节名** | `ai-skill-governor` 的 Auto-Fix Rules 示例把 `ai-frontend-availability-audit` 当作旧名（当前为 `ai-frontend-audit`，`skills/governance/ai-frontend-availability-audit` 不存在）；重叠检测方法要求比较 "Purpose \| 用途" 小节，而仓库内 SKILL.md 实际只有 `## Purpose` | 更新示例名与小节名 |
| F5 | **`ai-rule-dispatcher` 三条断链引用** | `rules/project_rules.md` 不存在（`rules/` 下只有 `AGENTS.md`、`AGENTS.global.md`、`rules.json`）；`ai-market-researcher` 不存在（只有 `SKILL_MANIFEST.json` 的 `rawOnlySkills` 里的 `gerp-market-researcher`）；`{stack}-frontend-dev` / `{stack}-backend-dev` 是占位符而非真实 Skill 名 | 修正为 `ai-competitor-analyst` / `ai-reference-researcher` 等真实名 |
| F6 | **正文过时、配套参考文件已更新（且正文从不提及该文件）** | `skills/core/ai-rule-dispatcher/references/routing-map.md` 已把竞品对标主责修正为 `ai-competitor-analyst`（+ 支撑 `ai-reference-researcher`），且不再列 `rules/project_rules.md`；但 SKILL.md 正文未同步、也未提及该 references 文件存在。同类：`ai-task-decomposer/references/decomposition-template.md` 亦未被正文引用 | 正文同步参考文件，并在 SKILL.md 中显式引用 references |
| F7 | **分类标题与表格内容不符、列义错位** | `skills/core/ai-tool-bootstrapper/SKILL.md:40` 标题写 `(npm/pip/cargo/composer)`，`:43–45` 表格只有 npm、pip、Playwright browsers（无 cargo/composer，且 Playwright 归错类）；`:69` 标题写 `(Playwright, Puppeteer, Selenium)`，`:72–74` 表格只有 Playwright、Edge fallback、Chrome（无 Puppeteer/Selenium，且 Edge/Chrome 行把「兜底策略」填进了「检测方式」列） | 补行或改标题；修正列归属 |
| F8 | **命令入口写法不一致** | `ai-tool-bootstrapper` 正文用 `python scripts/py/tool_registry.py`，而 `AGENTS.md` 规定用 `py scripts/py/tool_registry.py`（Windows 用 `py`） | 统一为 `py`，或注明两平台差异 |
| F9 | **引用了仓库中未定义的目录类别** | `skills/core/ai-foundation-governor/SKILL.md:61` 提到 `patch/`、`templates/`；全仓库检索 `patch/` 只命中该文件自身，`methodology/08_项目文件夹结构标准.md` 只定义了 `schema/`、`seed/` | 在 08 号文档补齐定义，或删除这两个类别 |
| F10 | **过期命令写法** | `ai-command-executor` 的命令分类表用 `docker-compose up`（Compose V1；V2 为 `docker compose up`） | 更新为 V2 写法 |
| F11 | **frontmatter 普遍缺版本字段** | 本轮处理的 49 个 SKILL.md 中，绝大多数只有 `name` + `description`，无 `version`；仅 `## Evolution History` 内有 `v1.0.0`、`v1.1.0` 等条目 | 由 `ai-skill-governor` 的 STALE 检查依赖 `version`/`updated` 字段，缺失会导致过期检测失效 |
| F12 | **`## Rule` 与 H1 的相对顺序跨文件不统一** | `ai-component-standardizer`、`ai-flow-closure-audit`、`ai-frontend-audit`、`ai-reference-researcher`、`ai-runtime-verify`、`ai-library-first`、`ai-tool-bootstrapper`、`ai-skill-governor`、`ai-atomic-governance` 等把 `## Rule` 写在 H1 之前；其余在 H1 之后 | 统一模板顺序 |

## F. references 配套文件与文件字节层面的缺陷（逐条已核实）

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| F1 | **3 篇 references 存在「双 CR」字节（`0D 0D 0A`）并与单独 LF 混排** | 字节级实测：`skills/core/ai-architect-governor/references/architecture-map.md` 59 处双 CR + 10 处单独 LF；`skills/governance/ai-flow-closure-audit/references/closure-checklist.md` 51 处 + 9 处；`skills/core/ai-skill-evolver/references/evolution-checklist.md` 23 处 + 13 处 | 统一为 LF（或统一 CRLF），清掉重复 CR |
| F2 | **多个 references 文件 CRLF/LF 混排** | `ai-command-executor/references/entrypoints.md` CRLF 54 / 单独 LF 27；`ai-rule-dispatcher/references/routing-map.md` 47/35；`ai-task-decomposer/references/decomposition-template.md` 69/8 | 同上 |
| F3 | **对项目侧资产的断链引用** | `entrypoints.md` 引用 8 个 `scripts/bat/*.bat`（含 `gerp.bat`）与 `scripts/ps1/*-functions.ps1`，但 `scripts/bat/` 只有 `.gitkeep`、`scripts/ps1/` 只有 install/probe/verify 类脚本；`scripts/logs`、`scripts/bat/logs/error`、`database/` 均不存在 | 明确这些是本项目资产还是 GERP 侧资产；若为 GERP 侧，应移出公开 Skill 树或用占位说明 |
| F4 | **空目录却被 3 处引用** | `docs/业务流程全案/` 实测 0 个文件，但 `routing-map.md`、`decomposition-template.md`、`closure-checklist.md` 都引用其下的“当前任务包” | 补 `_templates` 或改为占位路径 |
| F5 | **1 篇 references 明显过期** | `skills/governance/ai-ui-ux-governor/references/gerp-context.md` 的 mtime 为 2026-05-20（13 篇中最旧），其中 UI 约定（`ReportTemplatePage`、220px 分类树、每页 20 条等）绑定具体项目时点 | 复核是否仍与当前项目一致，或标注适用时点 |
| F6 | **1 处尾随空白** | `skills/governance/ai-ui-ux-governor/references/gerp-context.md:56` 行尾有多余空格 | 清理 |
| F7 | **文件命名与结构风格不统一** | `evolution-checklist.md` 无小节编号，而同级的 `routing-map.md`、`decomposition-template.md`、`entrypoints.md` 使用 `## N.` 编号；`field-governance-checklist.md` 与 `verification-checklist.md` 的 H1 本身已是中英双语，其余 11 篇为纯英文 | 统一模板 |

## G. tech 层 Skill 内容准确性（已复核：6 条真坑 / 9 条过时 / 8 条撤回）

> ⚠️ **已逐条复核，请以 [`技术内容核查结论.md`](技术内容核查结论.md) 为准。** 本节初版由并行译者通读产出，随后我回到源文件**上下文**重新核对，有三点更正：
> 1. **行号不可用**：初版行号取自 PowerShell，受「双 CR 行尾」影响被放大约 2 倍（初版写的 `:663`、`:685` 等一律作废），已复核为**真实行号**。
> 2. **8 条属我过度解读，已撤回或降级**：`flutter-animations` 的 300ms「规则冲突」（规则原文限定 `for UI feedback`）、`springboot-patterns` 的「反代取错 IP」（文档自己已说明 `ForwardedHeaderFilter`）、`multi-stage-dockerfile` 的「NODE_ENV 写法错」（实为散文建议，非 Dockerfile 指令）、`tailwind-design-system` 的「十六进制颜色自相矛盾」（`meta[name="theme-color"]` 必须用字面量）与「OKLCH 强度不一致」、`docker-expert` 的「规则与示例自相矛盾」（`node:18-alpine` 并非 `latest`）、`vue` 的「与官方冲突」（上游偏好，非错误）、`tailwind-design-system` 的「arbitrary values 自违」（`@utility` 定义值属正常写法）。
> 3. **新增一类发现**：10 / 20 个 tech Skill 存在 `0D 0D 0A`  **双 CR 行尾**，会让按行处理的工具（含校验脚本、diff、CI 行级检查）出现行号与行数偏差。

### G.1 真坑：照抄会失败或不生效（6 条）

| Skill | 真实行号 | 内容 | 后果 |
|---|---|---|---|
| `mysql-best-practices` | **L343** | `innodb_buffer_pool_size = 70%_of_RAM` | 取值非法（该参数取字节数，可带 `K`/`M`/`G`），配置无法解析；按内存自动配置应用 `innodb_dedicated_server` |
| `mysql-best-practices` | **L353-354** | `query_cache_type = 0` | MySQL 8.0 已删除该参数，**保留在 `my.cnf` 会导致实例无法启动**（上一行注释已写「disabled in MySQL 8.0+」，但未分版本给出写法） |
| `tailwind-css-patterns` | **L585-586** | JS 配置里的 `jit: true` | JIT 自 v3 起默认且该选项被移除，v4 已无 JS 配置；此行无效且误导 |
| `tailwind-css-patterns` | **L593-594** | `class="content-visibility-auto"` | 与本文件 **L744** 自定义的 `@utility content-auto` 类名不一致，core 也无内置类 → 复制过去零效果 |
| `tailwind-css-patterns` | **L602** | `class="contain-layout"` | core 无此 utility（社区另做插件），示例未说明需插件或自定义 → 零效果 |
| `flutter-animations` | **L606-612** | 声明 `assets/templates/` 下 4 个 `.dart` 模板 | 该技能目录只有 `SKILL.md` 与 `references/`，**没有 `assets/`** → 按文档找模板会落空 |

### G.2 真实但只是过时 / 兼容性 / 命名（9 条，能用）

`mysql-best-practices` L269 `SHOW SLAVE STATUS`（8.0.22 起弃用）、L344 `innodb_log_file_size`（8.0.30 起弃用）、L74 `TINYINT(1)` 显示宽度（8.0.17 起弃用）；`docker-expert` / `multi-stage-dockerfile` 示例基础镜像 `node:18(-alpine)`（Node 18 已于 2025-04-30 结束维护）；`docker-expert` L75 `docker-compose config`（Compose V1 可执行文件）；`docker-expert` L30 与 `flutter-expert` frontmatter 交接/引用了 `kubernetes-expert`、`react-native-expert`、`test-master`、`fullstack-guardian` 等不在 49 个正式 Skill 内的名字；`vue-best-practices` L31-32 软引用两个不存在的 Skill；`vue-pinia-best-practices` 用 `reference/`（单数）而另两个 Vue Skill 用 `references/`（复数）；**16 / 20** 个 tech Skill 的 frontmatter 无 `version`/`updated` 字段（`audit_skill_health.py` 的 180 天过期检测因此失效）。

逐条证据、外部依据链接（MySQL 官方文档、Tailwind 社区插件、Node 生命周期）与完整撤回理由见 [`技术内容核查结论.md`](技术内容核查结论.md)。

## H. 总控索引登记缺失（MASTER_INDEX / OPEN_SOURCE_PACKAGE）

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| H1 | **「目录为空」表述不准确** | `MASTER_INDEX.md` 第 3 节写 `skills/platform/` 当前为空，实际该目录含 4 个平台集成 Skill（`cnb-api`、`cnb-code-commit`、`cnb-code-review`、`cnb-pipeline`）；它们在 `SKILL_MANIFEST.json` 的 `rawOnlySkills` 中，`counts.byLayer.platform=0` | 改为「Platform 层当前无**正式** Skill」 |
| H2 | **Governance 表少一行** | 第 3 节合计写 13，紧随的表只列 12，缺 `ai-atomic-governance`（目录存在，属 49 个正式 Skill） | 补行（本轮中文版**未补**，保持与英文逐行对表，仅在译注登记） |
| H3 | **更新时间早于规则文件** | `MASTER_INDEX.md` 末尾「上次更新: 2026-08-21」，而 `rules/AGENTS.md` 为 2026-08-24 | 同步更新 |
| H4 | **多个已存在资产未登记** | 第 2 节未登记 `docs/全项目总控/CONSTITUTION.md`、`TASK_BACKLOG.md`、`AI_DELIVERY_SKILL_RESPONSIBILITY_MATRIX.md`、`pattern_ledger.json` 及 `docs/公开材料/FULL_AI_NATIVE_DEVELOPMENT_{WHITEPAPER,STANDARD}_{CN,EN}.md`；第 5 节未登记 `CUSTOMER_INVESTOR_VALUE.md`；第 6 节未登记 `audit_reference_links.py`、`tool_registry.py` | 补登记（MASTER_INDEX 自称是导航的唯一真相源） |
| H5 | **开源包清单有重复条目** | `OPEN_SOURCE_PACKAGE.md` 第 1 节中 `AI_NATIVE_DELIVERY_LOOP.md`（14、29 行）与 `DISCLOSURE_BOUNDARY.md`（16、30 行）各出现 2 次 | 去重 |
| H6 | **开源包清单未覆盖自身与两个门禁脚本** | 第 1 节未登记本文件自身，也未登记 `validate_delivery_contract.py`、`audit_ai_native_governance.py`；`FULL_AI_NATIVE_DEVELOPMENT_*` 4 篇的公开/私有归属未表态 | 明确归属 |

## I. 未纳入中文版的范围（说明，非缺陷）

| 项 | 原因 |
|---|---|
| `备用/`、`reference/`、`verification-demo/`、`temp/`、`docs/内部商业化/` 等 | 私有本地归档与内部材料，不属于公开方法论内核 |
| 20 个 tech 层 Skill 的正文 | 8 个源自上游第三方（`vue`、`vue-best-practices`、`vue-pinia-best-practices`、`flutter-expert`、`flutter-animations`、`docker-expert`、`springboot-patterns`、`springboot-security`），翻译正文等于与上游分叉；本轮只提供中文摘要 |
| `scripts/`、`.github/` 的代码与注释 | 工程资产，注释保持英文，避免与代码同步漂移 |
| `docs/公开材料/*_CN.md` | 源仓库已有中文版本，本轮直接引用不重译 |
