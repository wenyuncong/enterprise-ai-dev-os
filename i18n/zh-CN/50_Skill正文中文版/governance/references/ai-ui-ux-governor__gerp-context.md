# GERP 项目背景（UI/UX）| GERP Project Context (UI/UX)

> **源文件**：skills/governance/ai-ui-ux-governor/references/gerp-context.md
> **源版本**：未标注
> **译文日期**：2026-09-14
> **术语依据**：00_导读/02_术语对照表.md
> **翻译口径**：全量对应；英文标识符、命令、代码块、门禁代号原样保留。
> **说明**：本文件是 Skill `ai-ui-ux-governor` 的配套参考文件中中文对照版，不是正式加载源。

---

技术栈：

- Vue 3 + Vite + TypeScript
- Element Plus UI
- 使用中的字体（见 `Login.vue`）：HarmonyOS Sans SC、MiSans、Source Han Sans SC
- 主题初始化：`frontend/gerp-web/src/theme`（复用现有令牌）

约束：

- 使用项目基础组件（`BasePage`、`BasicDataLayout`、`BaseTable`、`PageToolbar`、`ConditionBar` 等）。
- 优先使用 Composition API 配合 `<script setup>`。
- 避免引入新的 UI 库；使用 Element Plus 与现有样式。
- 保持增量修改；避免大范围重写。
- ERP 业务页面是行动优先：先暴露工具条、条件栏、表格、表单、主要结果、操作入口与状态语义，再考虑解释性文案。
- 业务语义应当由按钮、操作列、状态标签、关联单据链接、快捷键与抽屉承载，而不是长段落。
- 禁止在列表 / 历史页以及其他数据密集的 ERP 界面中加入大块 hero 区域、长篇引导文字或超大解释区块。
- 引导内容应当压缩为一行提示、tooltip、抽屉或帮助入口；禁止让文字占据 ERP 操作页的首屏或取代交互结构。
- 登录页可以使用简短的信任 / 品牌文案，但该例外不得扩展到普通业务页面。
- 表格密集页面应当优先保留数据展示空间：通过共享令牌或基础组件，压缩区块间距、分类树与表格间距、筛选容器内边距、分页容器内边距与页面留白。除非基础组件确实无法表达该页面类型，否则禁止用页面本地覆盖来解决密度问题。
- 当用户点击某个操作，而该操作被业务规则、权限、版本限制、只读状态、上游数据缺失或路由标识缺失阻断时，前端必须使用 `feedback.blocking(...)`，让用户看到带显式确认按钮的模态框。禁止把这些情况降级为会消失的轻提示（Toast）。校验错误、空数据导出提醒、重复行提示与降级兜底通知应当保持为警告或行内反馈。

需要检查的入口：

- 登录：`frontend/gerp-web/src/components/Login.vue`
- 主题：`frontend/gerp-web/src/theme`
- 布局：检查现有布局组件是否存在（`BasePage` / `BasicDataLayout`）。

---

## 报表/统计页面规范（强制）

1. **模板选用**: 统计报表页统一使用 `ReportTemplatePage`，不得使用 `BasePage` 或 `BasicDataLayout`。传 `page-key`（`ReportTemplatePage` 的约定 prop）。
2. **维度切换**: 多维度统计（按商品/客户/品牌/区域等）使用 `SecondaryTabs` 承载，Tab 切换时保留筛选条件上下文（日期范围等公共参数）。
3. **默认视图**: 报表页打开默认展示**表格**视图，图表视图作为 `view-options` 中的辅助切换项。
4. **表格要求**: 必须包含序号列（`:show-index="true"`）、数量列（可点击下钻到明细）、金额列、合计行（`footer-summary` slot）。分页必须开启（默认每页 20 条）。
5. **分类筛选**: 商品/客户维度报表页须在左侧提供分类树侧边栏筛选（使用分类树数据 + 前端本地过滤），宽度 220px，不参与服务端分页。
6. **筛选条件复用**: 各维度 Tab 共享日期范围、品牌、仓库等公共筛选条件，维度特有筛选（如"来源"维度的 `sourceType`）仅在其 Tab 内出现。

## 表格单元格可点击下钻

1. **标示方式**: 可点击下钻的列在列配置中设置 `cellClickable: true`。
2. **触发方式**: 点击单元格本身即可跳转，不额外放置图标或按钮。
3. **下钻目标**: 数量列点击后跳转到对应明细查询页（如 `SalesDetailQuery`），自动带入维度筛选条件（商品 ID / 客户 ID / 品牌 ID / 仓库 ID / 职员 ID + 时间范围）。
4. **视觉反馈**: 可点击列的文字颜色使用主题变量（源文件此处为硬编码的十六进制颜色字面量，译文按中文版规范改写为「主题变量」），hover 时加下划线。

## 组件缺失时的扩展原则

1. **优先级**: 项目封装组件 → Element Plus 原生组件 → 自建新组件。
2. **使用原生组件前提**: 确认项目 `components/` 下无对应封装，并在代码中标注 `<!-- TODO: 待封装为项目组件 -->`。
3. **新组件放置**: `components/base/`（通用基础组件）或 `components/<module>/`（业务专用组件）。
4. **禁止大面积绕过**: 一个页面中直接使用 Element Plus 原生组件的次数不得超过 3 处，超过则必须先封装。
---

## 生产计划工作台规范

1. **页面定位**: 生产计划页为"生产计划工作台"，汇集销售驱动、预测补货、手工排产、BOM/MRP驱动、订单驱动等多来源计划。
2. **下推链路**: 
    - 生产计划（已审核/已下达）→ 生成生产工单（`ProductionOrder`）
    - 生产计划（已审核/已下达，且来源为 BOM/销售/订单）→ 生成采购申请（根据 BOM 计算物料短缺）
3. **仪表盘**: 顶部摘要卡展示待审核/已审核/执行中/已完成/计划量/合计，物料短缺时显示预警条。
4. **高可用性要求**:
    - 所有异步操作必须有 `try/catch` + `feedback.error`
    - 数据加载失败自动重试（最多 3 次，间隔递增）
    - 提供"重试"和"忽略"按钮
    - 搜索条件变化时 300ms 防抖
    - 组件卸载时清理所有 timer

---

## 译注 | Translation Notes

1. **本文件属项目背景，非通用方法论**：本文件面向具体的 GERP 企业项目（Vue 3 + Element Plus 技术栈、`frontend/gerp-web` 目录、生产计划工作台等），不属于跨项目通用方法论；翻译按原文照实对应，未做通用化改写。
2. **被改写的片段（1 处）**：
   - 原文「可点击列的文字颜色使用一个十六进制颜色字面量（Element Plus 主题蓝），hover 时加下划线」→ 改写为「主题变量」表述。改写原因：中文版规范禁止译文出现硬编码颜色字面量，要求统一表述为主题变量；本说明行同样不再写出该字面量。
3. **未发现需脱敏的内容**：原文未出现盘符绝对路径、真实客户名、内网地址或凭据。
4. **保留的项目内部相对路径**：`frontend/gerp-web/src/theme`、`frontend/gerp-web/src/components/Login.vue`、`components/base/`、`components/<module>/` 均为仓库内相对路径（不含盘符），按禁译清单原样保留。若对外发布需要脱敏，可整体改写为 `{project_path}` 形式的占位符。
5. **项目代号 `GERP`**：源文件名与仓库多处官方引用均使用 `GERP` 作为企业项目代号，故按专有代号原样保留，不替换为 `<目标项目>`；如需对外脱敏，建议整册统一替换。
